# 加 logs_bak 表（外接磁盘）· 安全操作手册

> 环境：MySQL(A服 `147.135.76.13`) → Debezium → Kafka → ClickHouse(B服 `40.160.14.146` 宿主机原生)
> 连接器名：`yibuapi-connector` ｜ CK 库：`yibuapi` ｜ docker 栈目录：`~/cdc/docker`
>
> **核心原则：先配好磁盘，再建表。** 表一旦建好，存储位置就改不了了（只能重建+迁数据）。

---

## 本次任务已确认的事实

| 项 | 值 | 状态 |
|---|---|---|
| 新表 | `yibuapi.logs_bak` | 2.49 亿行 / 数据 163GB / 索引 152GB(不同步) |
| 列结构 | 21 列，与已同步的 `logs` **完全一致** | 无新类型风险 |
| 主键 | `id` (bigint PRI) | — |
| 分区列 | `created_at` bigint **秒级**，无 NULL | `toYYYYMM(toDateTime(created_at))` |
| 数据时间跨度 | 2026-05-31 ~ **2026-07-24**（已停止写入） | 静态归档表，快照完基本就结束 |
| 分区数 | 3 个（05/06/07月） | 合理 |
| binlog 保留 | 604800 秒 = **7天**，写入量仅 316MB/天 | ✅ 风险极低 |
| gtid_mode | **OFF** → 位点是 file+pos 形式 | ⚠️ 期间勿做 MySQL 主从切换/换实例 |
| 系统盘 `/dev/sda1` | 387G，可用 **185G**（含 CK 35G + Kafka卷） | 快照期间要盯 |
| 外接盘 `/dev/sdb` | 1000G，xfs，**已挂载** `/data/ck_disk1`，可用 981G | 阶段1 基本可跳过 |

**代码改动已完成**（本地已改并验证，36 张表渲染正常、35 张老表 DDL 零变化）：
- `cdc_sync/config.py` — `TableDef` 增加 `disk` / `storage_policy` / `partition_unit`
- `cdc_sync/type_map.py` — 新增 `is_int_type()`
- `cdc_sync/ck_generator.py` — 整数时间戳分区 + `_storage_settings()`
- `templates/ck_target.sql.j2` — 渲染 `SETTINGS`
- `config/tables.yaml` — 追加 `logs_bak`（末尾，不影响老表）

> 本次范围**仅 logs_bak 放外接盘**。系统盘整体扩容（CK 数据目录/Kafka 卷迁移）另议。

---

## ⛔ 三条红线（先记住再动手）

| 禁止 | 后果 |
|------|------|
| **不要跑 `bash reset.sh`** | 内含 `docker compose down -v`，**删掉 kafka-data 卷** → binlog位点/快照进度/schema历史/CK消费位点**全部丢失**，所有表从头重来 |
| **不要跑任何 `docker compose down -v`** | 同上。`down` 不带 `-v` 是安全的 |
| **不要点面板「开启同步」按钮** | 它会对 tables.yaml 里**全部 36 张表**发增量快照信号（全量重刷）。必须用 `snapshot --only logs_bak` |

> 补充：`update.sh --boot` 也别用（会跑 bootstrap 并把连接器 pause 掉）。
> 补充：`gtid_mode=OFF`，位点绑定当前 MySQL 实例，**操作期间勿做主从切换**。

---

## 阶段 0 · 前置检查 ✅ 已全部完成

三项检查结论见上表，均通过。binlog 7 天 + 写入量 316MB/天，位点失效风险极低。

---

## 阶段 1 · 确认外接盘（已挂载，只需建目录）

盘已经挂好了（`/dev/sdb` xfs → `/data/ck_disk1`，fstab 应已配置），**不用格式化、不用 mount**。

### 1.1 先查清那 20G 是什么 ⚠️ 未确认，别跳过

```bash
sudo ls -la /data/ck_disk1/
sudo du -sh /data/ck_disk1/* 2>/dev/null
ls /etc/clickhouse-server/config.d/
grep -rl "ck_disk1" /etc/clickhouse-server/ 2>/dev/null || echo "(CK配置未引用该盘)"
```

> 如果 CK 配置里**已经定义过**这块盘，记下磁盘名，阶段 2 可直接复用、跳过建 xml。

### 1.2 建 CK 数据目录 + 权限

```bash
sudo mkdir -p /data/ck_disk1/clickhouse
sudo chown -R clickhouse:clickhouse /data/ck_disk1/clickhouse
sudo chmod 750 /data/ck_disk1/clickhouse
ls -ld /data/ck_disk1/clickhouse          # 属主必须是 clickhouse
```

### 1.3 确认 fstab 里有 nofail

```bash
grep ck_disk1 /etc/fstab
```
没有 `nofail` 就加上（防止盘掉了系统起不来）：
```
UUID=<blkid /dev/sdb 拿到的>  /data/ck_disk1  xfs  defaults,nofail  0  2
```

---

## 阶段 2 · 配置 ClickHouse 磁盘（B 服）

新建 `/etc/clickhouse-server/config.d/storage.xml`：

```xml
<clickhouse>
  <storage_configuration>
    <disks>
      <ext_disk>
        <path>/data/ck_disk1/clickhouse/</path>
        <!-- 末尾斜杠不能少！留 20GB 余量防写满 -->
        <keep_free_space_bytes>21474836480</keep_free_space_bytes>
      </ext_disk>
    </disks>
  </storage_configuration>
</clickhouse>
```

> 本次**只需定义 disk，不需要 policy** —— 外接盘 981G 可用，单盘直接装得下，
> 建表用 `SETTINGS disk = 'ext_disk'` 即可，配置最少、最不容易出错。

```bash
sudo chown clickhouse:clickhouse /etc/clickhouse-server/config.d/storage.xml
```

热加载（**不用重启 CK**）：
```bash
clickhouse-client --password '<CK密码>' -q "SYSTEM RELOAD CONFIG"
```

**验收**（必须看到 `ext_disk` 才继续）：
```bash
clickhouse-client --password '<CK密码>' -q "
SELECT name, path, formatReadableSize(free_space) AS free,
       formatReadableSize(total_space) AS total
FROM system.disks FORMAT PrettyCompact"
```
期望看到 `ext_disk | /data/ck_disk1/clickhouse/ | ~960 GiB | ~1000 GiB`

> ❗ 看不到就查日志：`sudo tail -100 /var/log/clickhouse-server/clickhouse-server.err.log`
> 常见原因：path 末尾少斜杠 / 目录属主不是 clickhouse / xml 语法错。

> CK 密码取自：`grep CK_PASSWORD ~/cdc/docker/.env`

---

## 阶段 3 · 备份当前断点（几秒钟，强烈建议）

```bash
cd ~/cdc/docker
TS=$(date +%F_%H%M)

# 1) 备份连接器 offset（binlog位点+快照进度都在这）
docker compose exec -T kafka kafka-console-consumer \
  --bootstrap-server localhost:9092 --topic _cdc_connect_offsets \
  --from-beginning --timeout-ms 15000 > ~/offsets_backup_$TS.txt 2>/dev/null

# 2) 备份连接器配置
docker compose exec -T connect curl -s localhost:8083/connectors/yibuapi-connector/config \
  > ~/connector_backup_$TS.json

# 3) 备份表清单
cp ~/cdc/config/tables.yaml ~/tables_backup_$TS.yaml

# 4) 记录基线（事后对比用）
docker compose exec -T connect curl -s localhost:8083/connectors/yibuapi-connector/status
docker compose exec -T cdc-monitor python main.py reconcile > ~/reconcile_before.txt
cat ~/reconcile_before.txt
```

---

## 阶段 4 · 拉取代码（改动已在本地完成，需推 GitHub）

```bash
cd ~/cdc/docker
bash update.sh                       # git pull + 只重建 cdc-monitor，不碰 connect，位点安全

# 验证新代码进容器了（三条都应 ≥1）
docker compose exec -T cdc-monitor grep -c "storage_settings" cdc_sync/ck_generator.py
docker compose exec -T cdc-monitor grep -c "partition_unit"   cdc_sync/config.py
docker compose exec -T cdc-monitor grep -c "logs_bak"         config/tables.yaml
```

> `tables.yaml` 已随代码一起改好（追加在 midjourneys 之前），**不需要再手工登记**。
> 阶段 5（登记新表）因此可以跳过。

---

## 阶段 5 · ~~登记新表~~ → 已随代码完成，跳过

`config/tables.yaml` 里已追加 `logs_bak`，含 `order_by` / `partition_by` / `partition_unit` / `disk`。
面板「配置」页应能看到表数 **36**。

---

## 阶段 6 · 建 CK 表（关键步骤）

```bash
cd ~/cdc/docker

# 1) 先 dry-run 看生成的 SQL（⚠️ 必看，别跳过）
docker compose exec -T cdc-monitor python main.py apply-ck --dry-run 2>&1 | grep -A 30 "logs_bak"
```

**必须确认输出里有这三行**：
```
ORDER BY (`created_at`, `id`)
PARTITION BY toYYYYMM(toDateTime(`created_at`))
SETTINGS disk = 'ext_disk'          ← 最关键，没有就是白干
```
以及 `` `created_at` Int64 ``（**不带 Nullable**，否则分区会失败）。

确认无误后执行：
```bash
docker compose exec -T cdc-monitor python main.py apply-ck
```

> 35 张老表是 `CREATE TABLE IF NOT EXISTS`，会被跳过，DDL 已验证零变化。

**验收 — 确认真的落在外接盘**：
```bash
clickhouse-client --password '<CK密码>' -q "
SELECT name, storage_policy, data_paths FROM system.tables
WHERE database='yibuapi' AND name='logs_bak' FORMAT Vertical"
```
`data_paths` 必须指向 `/data/ck_disk1/clickhouse/...`

> ❗ 如果指向 `/var/lib/clickhouse/`，说明 SETTINGS 没生效。
> **立刻 `DROP TABLE yibuapi.logs_bak` 重来** —— 现在删还没成本，灌了 2.49 亿行就麻烦了。

---

## 阶段 7 · 给新表 topic 设 retention（防系统盘爆）

163GB 数据要流经 Kafka，`kafka-data` 卷在**系统盘**（可用 185G）。

```bash
cd ~/cdc/docker

docker compose exec -T kafka kafka-topics --bootstrap-server localhost:9092 \
  --create --if-not-exists --topic mysql.yibuapi.logs_bak \
  --partitions 1 --replication-factor 1

# 4小时 + 80GB 上限（系统盘 185G 可用，留足余量）
docker compose exec -T kafka kafka-configs --bootstrap-server localhost:9092 \
  --alter --entity-type topics --entity-name mysql.yibuapi.logs_bak \
  --add-config retention.ms=14400000,retention.bytes=85899345920

docker compose exec -T kafka kafka-configs --bootstrap-server localhost:9092 \
  --describe --entity-type topics --entity-name mysql.yibuapi.logs_bak
```

> ⚠️ **只对这一个 topic 设**，千万别改全局 `log.retention.ms` —— 那会把
> `_cdc_connect_offsets` 和 `schema-changes.yibuapi` 一起清掉，位点直接没了。
>
> ⚠️ **retention 是兜底，不是保护**：Kafka 删段不管消费者读没读完。设 80G（而非更小）
> 就是为了给 CK 留足消费时间。**主要靠阶段 9 盯 lag**，别指望 retention 救你。

---

## 阶段 8 · 更新连接器（老表位点安全）

```bash
cd ~/cdc/docker

# 1) dry-run 确认 include.list 多了 logs_bak、其他配置没变
docker compose exec -T cdc-monitor python main.py deploy-connector --dry-run | grep -i "include.list"

# 2) 发布（走 PUT /config 更新，不是删除重建）
docker compose exec -T cdc-monitor python main.py deploy-connector
```

**原理**：Debezium 的 offset key 只跟 `topic.prefix`(`mysql`) 和 `database.server.id`(`1001`) 绑定，
**跟 `table.include.list` 无关**。task 重启几十秒后从 `_cdc_connect_offsets` 读回原位点，
**35 张老表继续从断点走，不会重新快照**。

**验收**（等 30~60 秒，必须 `RUNNING` 才继续）：
```bash
docker compose exec -T connect curl -s localhost:8083/connectors/yibuapi-connector/status
# 期望：{"connector":{"state":"RUNNING"},"tasks":[{"id":0,"state":"RUNNING"}]}
```

---

## 阶段 9 · 只对 logs_bak 做快照回填 ⚠️ 最容易出错的一步

```bash
cd ~/cdc/docker

# ✅ 正确
docker compose exec -T cdc-monitor python main.py snapshot --only logs_bak

# ❌ 不加 --only → 对全部 36 张表发信号，全量重刷
# ❌ 点面板「开启同步」→ 同样是全表快照
```

**预计耗时**：2.49 亿行 ÷ 32768/块 ≈ **7600 个块**，数小时到一天。

**三个监控窗口（建议 tmux 分屏常开）**：
```bash
# ① 磁盘水位 —— 最重要，系统盘爆了会同时搞挂 Kafka 和 Connect
watch -n 60 'df -h / /data/ck_disk1'

# ② Kafka 堆积（lag 持续涨说明 CK 消费跟不上，可能触发 retention 丢数据）
watch -n 60 'docker compose exec -T kafka kafka-consumer-groups \
  --bootstrap-server localhost:9092 --describe --group ck-logs_bak-consumer-group'

# ③ CK 行数增长
watch -n 60 "clickhouse-client --password '<CK密码>' -q \
  \"SELECT count() FROM yibuapi.logs_bak\""
```

快照日志：
```bash
docker compose logs -f connect | grep -i -E "snapshot|chunk"
```

> **中断了不用慌**：增量快照分块进行，位点在 Kafka 里，重发一次
> `snapshot --only logs_bak` 会从上次的块继续，不会从头。

---

## 阶段 10 · 验收

```bash
cd ~/cdc/docker

# 1) 全表对账，对比基线确认老表没退化
docker compose exec -T cdc-monitor python main.py reconcile | tee ~/reconcile_after.txt
diff ~/reconcile_before.txt ~/reconcile_after.txt

# 2) 连接器 + CK 消费状态
docker compose exec -T cdc-monitor python main.py status
```

```bash
clickhouse-client --password '<CK密码>' -q "
-- 确认落盘位置 + 实际占用（这才是 logs_bak 真实体积）
SELECT disk_name, count() AS parts, sum(rows) AS rows,
       formatReadableSize(sum(bytes_on_disk)) AS size
FROM system.parts WHERE database='yibuapi' AND table='logs_bak' AND active
GROUP BY disk_name;

-- 分区分布（应为 3 个：202605/202606/202607）
SELECT partition, sum(rows) AS rows, formatReadableSize(sum(bytes_on_disk)) AS size
FROM system.parts WHERE database='yibuapi' AND table='logs_bak' AND active
GROUP BY partition ORDER BY partition;

-- 行数（正式表查询必须带 FINAL + is_deleted=0）
SELECT count() FROM yibuapi.logs_bak FINAL WHERE is_deleted = 0;
"
```

**期望**：`disk_name = ext_disk`，行数 ≈ 2.49 亿，3 个分区。

> `logs_bak` 是静态归档表（2026-07-24 后无新写入），所以**不需要做实时性验证**。
> 快照跑完、对账一致即完成。

---

## 出问题了怎么办

| 现象 | 处理 |
|------|------|
| `system.disks` 看不到 ext_disk | 查 CK err 日志；多半是 path 少末尾斜杠 / 目录属主不是 clickhouse / xml 语法错 |
| 建表后 `data_paths` 指向 /var/lib | SETTINGS 没生效。**立刻 DROP TABLE 重来**，别灌数据 |
| 建表报 Nullable 不能做分区键 | `created_at` 没进 ORDER BY。检查 tables.yaml 的 `order_by` 是否为 `` `created_at`, `id` `` |
| 连接器 task FAILED | `docker compose logs --tail=300 connect` 看堆栈。位点还在，修好重启即可 |
| `binlog ... no longer available` | 位点失效（binlog被purge / 换了MySQL实例）。**别急着 reset**，先评估能否只重刷受影响的表 |
| Kafka lag 一直涨不降 | CK 消费跟不上。可临时提高 `kafka_max_block_size`，或暂停连接器让 CK 追一会 |
| 系统盘快满 | 缩短 logs_bak topic 的 retention.ms；或 `kafka-topics --delete` 掉已消费完的临时 topic |
| 外接盘写满 | 已留 20GB `keep_free_space_bytes`。可 `ALTER TABLE ... MOVE PARTITION ... TO DISK 'default'` 腾挪 |

**回滚**（老表完全不受影响）：
```bash
CKPW=$(grep CK_PASSWORD ~/cdc/docker/.env | cut -d= -f2)
clickhouse-client --password "$CKPW" -q "DROP TABLE IF EXISTS yibuapi.mv_logs_bak"
clickhouse-client --password "$CKPW" -q "DROP TABLE IF EXISTS yibuapi.logs_bak_kafka"
clickhouse-client --password "$CKPW" -q "DROP TABLE IF EXISTS yibuapi.logs_bak"

cp ~/tables_backup_*.yaml ~/cdc/config/tables.yaml
cd ~/cdc/docker && docker compose exec -T cdc-monitor python main.py deploy-connector
```

---

## 速查：一页纸流程

```
0. ✅ 前置检查已完成（binlog 7天 / created_at秒级无NULL / 外接盘981G可用）
1. 查 /data/ck_disk1 那 20G 是什么 → 建 clickhouse 子目录 + chown
2. storage.xml 定义 ext_disk → SYSTEM RELOAD CONFIG → 验 system.disks ✅
3. 备份 offsets / connector config / tables.yaml / reconcile 基线
4. bash update.sh 拉代码 → 验 3 个 grep
5. （跳过，tables.yaml 已随代码改好）
6. apply-ck --dry-run 确认 SETTINGS disk='ext_disk' → apply-ck → 验 data_paths ✅
7. logs_bak topic 设 retention.ms=4h / retention.bytes=80G
8. deploy-connector → 等 task RUNNING ✅
9. snapshot --only logs_bak   ← 千万别漏 --only，别点面板按钮
   开 3 个 watch：df -h / kafka lag / CK 行数
10. reconcile 对比基线 / 验 disk_name=ext_disk / 验 3 个分区
```

---

## 遗留事项（本次不做）

**系统盘才是长期矛盾**：387G 里 202G 已用，CK(35G) + Kafka卷 + 其余 35 张表全在上面，
外接盘 981G 却基本空转。本次只把 `logs_bak` 放过去，没解决根本问题。

后续可考虑（择一）：
- **整个 CK 数据目录迁到大盘**（`rsync` + 改 path.xml，停机 10~20 分钟，一劳永逸，之后所有表自动受益）
- **Docker kafka-data 卷迁到大盘**（消除快照期间的爆盘风险）
- 加 storage_policy 让大盘成主卷（不停机，但老表要逐个 ALTER + MOVE PARTITION，且
  `ALTER TABLE MODIFY SETTING storage_policy` 在 CK 26.x 的行为未实测，需先拿小表验证）

