# CDC 同步栈 · 运维命令手册

> 架构：MySQL(A服 `147.135.76.13`) → Debezium → Kafka → ClickHouse(B服 `40.160.14.146` 宿主机原生)
> 本栈(Kafka+SchemaRegistry+Connect+监控)用 docker 部署在 **B服 `40.160.14.146`**
> 监控面板：`http://40.160.14.146:8080`
> 代码仓库：`https://github.com/farmoun/cdc.git`

所有 `docker compose` 命令都要在 **`~/cdc/docker`** 目录下执行。

---

## 一、首次部署（B服，一次性）

```bash
git clone https://github.com/farmoun/cdc.git
cd cdc/docker
cp .env.example .env
vim .env                     # 改 MYSQL_PASSWORD、CK_PASSWORD（IP 已预填）

bash deploy.sh               # 完整部署：起栈 + 建CK表 + 注册连接器（停止态，不自动同步）
```
浏览器打开 `http://40.160.14.146:8080`：
1. 先到「配置」页各组件点「测试连接」确认全绿
2. 点右上角 **「开启同步」** 按钮才开始抓取数据

> **严格控制**：部署完一切就绪但**不会自动同步**。同步的开启/停止完全由面板右上角按钮控制。
> 命令行等价：`docker compose exec -T cdc-monitor python main.py start` / `stop`。

---

## 二、日常更新（我在 GitHub 改了代码后）

```bash
cd ~/cdc/docker
bash update.sh               # git pull + 重建监控容器（加载新代码）
```

变体：
| 命令 | 用途 |
|------|------|
| `bash update.sh` | 拉代码 + 重建监控（普通改代码/前端） |
| `bash update.sh --build` | **加了新依赖**时（如导出的 openpyxl/xlwt），重建镜像 |
| `bash update.sh --reseed` | 顺便按最新 `.env` 重新生成 `config/settings.yaml` |
| `bash update.sh --boot` | 重建后再跑一次 bootstrap（建表+发连接器） |

> 前端改了如果没生效：普通刷新即可（已加 no-cache）；还不行按 `Ctrl+Shift+R`。

---

## 三、换库 / 重置到原始状态

换 MySQL 源或读新数据库时，旧的 binlog 位点会失效（报 `binlog ... no longer available`），需要清空重来。

```bash
cd ~/cdc/docker
bash reset.sh                # 交互确认（输 yes）：删连接器+清位点、清CK库、清空Kafka卷、重起栈
# bash reset.sh -y           # 跳过确认
```

重置后换新库：
```bash
# 1) 改配置：监控页「配置」改 MySQL 地址+库名并保存；或改 .env 后：
bash update.sh --reseed
# 2) 导入新库表结构（配置页「导入SQL」，或命令行）：
docker compose exec -T cdc-monitor python main.py import-sql --file config/新库.sql --database 新库名
# 3) 重新初始化：
docker compose exec -T cdc-monitor python main.py bootstrap
```

只清空 ClickHouse 库（不动 Kafka）：
```bash
docker compose exec -T cdc-monitor python -c "import clickhouse_connect as c; cli=c.get_client(host='host.docker.internal',port=8123,username='default',password='12345678'); cli.command('DROP DATABASE IF EXISTS yibuapi'); cli.command('CREATE DATABASE yibuapi'); print('done')"
```

---

## 四、手动分步命令（脚本不方便时）

```bash
cd ~/cdc

# 拉最新代码（覆盖本地改动；.env/settings.yaml 被忽略不受影响）
git fetch origin && git reset --hard origin/main
git log --oneline -3                 # 看最新提交

cd docker
# 重建监控容器（加载挂载的新代码）
docker compose up -d --force-recreate cdc-monitor
# 重建监控镜像（加了依赖时）
docker compose up -d --build cdc-monitor

# 一键初始化：等Connect就绪 → 建35张CK表 → 发连接器
docker compose exec -T cdc-monitor python main.py bootstrap
# 部署后立即开启（默认部署为停止态）
docker compose exec -T cdc-monitor python main.py bootstrap --start
# 开启/停止同步（严格控制，等价于面板按钮）
docker compose exec -T cdc-monitor python main.py start
docker compose exec -T cdc-monitor python main.py stop
# 对账（MySQL vs CK 行数）
docker compose exec -T cdc-monitor python main.py reconcile
# 查看状态
docker compose exec -T cdc-monitor python main.py status
```

---

## 五、常用运维

```bash
cd ~/cdc/docker

docker compose ps                    # 各容器状态（看 healthy）
docker compose logs -f connect       # 连接器日志
docker compose logs -f kafka         # Kafka 日志
docker compose logs -f cdc-monitor   # 监控日志
docker compose restart connect       # 重启某服务

docker compose down                  # 停栈（保留数据卷）
docker compose down -v               # 停栈 + 清空 Kafka 数据（谨慎）
docker compose up -d                 # 起栈
docker compose up -d --build         # 起栈并重建镜像
```

---

## 六、连接器诊断（排 task 0/0、报错）

```bash
cd ~/cdc/docker

# 连接器状态（含 task 失败堆栈）
docker compose exec -T connect curl -s localhost:8083/connectors/yibuapi-connector/status

# 连接器配置
docker compose exec -T connect curl -s localhost:8083/connectors/yibuapi-connector/config

# 列出所有连接器
docker compose exec -T connect curl -s localhost:8083/connectors
```

正常应看到：`"tasks":[{"id":0,"state":"RUNNING"}]`

---

## 七、验证同步（在 B服 ClickHouse 执行）

```sql
-- 目标表行数（判断快照有没有灌进来）
SELECT count() FROM yibuapi.users;
SELECT count() FROM yibuapi.channels;

-- 查具体数据（正式表统一用 FINAL + 过滤软删）
SELECT * FROM yibuapi.checkins FINAL WHERE is_deleted = 0 AND user_id = 888888;

-- CK 消费状态 + 异常（换表名查其它）
SELECT database, table, num_messages_read, num_commits, exceptions.text
FROM system.kafka_consumers WHERE table = 'checkins_kafka' FORMAT Vertical;
```

> 说明：每张源表在 CK 有 3 个对象——`表名`(正式表，查这个) / `表名_kafka`(消费表) / `mv_表名`(物化视图)。只查**无后缀的正式表**。

在 A服 MySQL 造测试数据验证增删改：
```sql
INSERT INTO yibuapi.checkins (user_id, checkin_date, quota_awarded, created_at)
VALUES (888888, '2026-07-15', 100, UNIX_TIMESTAMP());
UPDATE yibuapi.checkins SET quota_awarded = 555 WHERE user_id = 888888;
DELETE FROM yibuapi.checkins WHERE user_id = 888888;
```

---

## 八、验证代码是否已进容器（更新后没生效时）

```bash
cd ~/cdc/docker
# 应输出 ≥1，否则说明代码没拉到或挂载没生效
docker compose exec -T cdc-monitor grep -c "AvroConfluent" cdc_sync/ck_generator.py
git -C ~/cdc log --oneline -1        # 确认拉到了最新提交
```

---

## 九、网页面板功能（`http://40.160.14.146:8080`）

| Tab | 功能 |
|-----|------|
| **监控** | 组件健康卡 / 连接器任务 / Kafka堆积(lag) / CK消费状态 / 对账；操作按钮：发布连接器、重启、建CK表、对账 |
| **查询** | 输入 SQL 执行(Ctrl+Enter)、结果表格、**导出 CSV/XLSX/XLS**、保存常用查询 |
| **配置** | 改连接配置(MySQL/CK/Kafka/SR/Connect/Debezium)、逐组件「测试连接」、编辑表清单、导入SQL |

---

## 十、常见故障速查

| 现象 | 原因 / 处理 |
|------|------------|
| 连接器 `task 0/0`，`Only a single connector task may be started` | Debezium MySQL 只能 1 个 task，`tasks.max=1`（已修） |
| CK 消费报 `Magic does not match` (AVRO_EXCEPTION) | CK 表要用 `AvroConfluent` 格式，非 `Avro`（已修） |
| `Unknown setting 'kafka_group_id'` | CK 设置名是 `kafka_group_name`（已修） |
| `binlog ... no longer available` | 换了 MySQL 源，旧位点失效 → `bash reset.sh` |
| 网页改了没生效 | 普通刷新（已加 no-cache）；加了依赖要 `bash update.sh --build` |
| CK 显示不可达 / 连不上 | CK 要监听 `0.0.0.0`；`.env` 的 `CK_HOST=host.docker.internal`(不通改 `40.160.14.146`) |
| 三个测试仍 `getaddrinfo failed` | 监控必须跑在 docker 里（容器版，端口 8080），别在 docker 外单独跑 |
| Schema Registry/Connect 测试连不上 | 是本栈自启的容器；栈起来后才通，内部名 `schema-registry:8081`/`connect:8083` 只在 docker 内可解析 |

---

## 十一、关键配置项（`.env`）

```ini
MYSQL_HOST=147.135.76.13          # A服 MySQL
MYSQL_USER=root
MYSQL_PASSWORD=12345678
CK_HOST=host.docker.internal      # B服宿主机CK（不通改 40.160.14.146）
CK_DATABASE=yibuapi
KAFKA_BROKER_FOR_CK=localhost:9094      # CK消费用(宿主机)
KAFKA_INTERNAL_BROKER=kafka:9092        # 容器内用
DBZ_CONNECTOR_NAME=yibuapi-connector
DBZ_SNAPSHOT_MODE=when_needed     # 位点失效自动重新快照(换服务器更省心)
```
