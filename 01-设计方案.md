# MySQL → Debezium → Kafka → ClickHouse 实时 CDC 同步 · 设计方案

> 版本：v1.0 ｜ 日期：2026-07-15
> 依据：《MySQL→Debezium→Kafka→ClickHouse 企业级实时 CDC 架构方案（完整版）》
> 定位：企业级高可靠、可扩展的 MySQL 实时同步 ClickHouse 数仓方案，替代原生 MaterializedMySQL

---

## 1. 设计目标与范围

### 1.1 设计目标

| 目标 | 量化指标（建议） |
|------|------------------|
| 端到端同步延迟 | P95 ≤ 5s，P99 ≤ 15s（正常负载） |
| 数据一致性 | 最终一致，事务级位点续跑，零丢失 |
| 可用性 | 任一组件单点故障不丢数据、可自愈 |
| 可扩展性 | 支持水平扩容 Connect/Kafka/CK，多库多表接入 |
| 可追溯性 | 支持基于 Kafka 位点的任意时间点重放 |
| 变更适配 | 全量 DML + 常见 DDL 自动/半自动适配 |

### 1.2 覆盖范围

- **在范围内**：MySQL 5.7/8.0 业务库的库/表级增量同步、全量快照初始化、DML/DDL 变更同步、软删除、多库多表/分库分表合并、监控告警、故障恢复与重放。
- **不在范围内（本期）**：跨机房双活、MySQL→MySQL 反向同步、复杂流式计算（Flink 层）。这些作为后续演进项，见 §12。

### 1.3 核心设计取舍

- 用「解耦 + 缓冲」换「组件数量与运维成本」：Kafka 作为持久化缓冲，容忍上下游任意一端故障。
- 用「双层表 + 物化视图 + ReplacingMergeTree」换「CK 侧的增删改语义」：以版本号合并 + 软删除标记取代 MySQL 的原地更新/删除。
- 用「Avro + Schema Registry」换「DDL 演进的鲁棒性」：统一 schema 演进，避免 JSON 字段乱序/缺失导致解析失败。

---

## 2. 总体架构设计

### 2.1 四层架构

```
┌─────────────┐   Binlog(ROW/FULL)   ┌──────────────────────┐
│  数据源层    │ ───────────────────▶ │  CDC 采集层           │
│  MySQL       │                      │  Debezium(KafkaConnect)│
└─────────────┘                      └──────────┬───────────┘
                                                │ Avro CDC 事件
                                                ▼
                                     ┌──────────────────────┐
                                     │  消息缓冲层            │
                                     │  Kafka + SchemaRegistry│
                                     └──────────┬───────────┘
                                                │ 消费
                                                ▼
                                     ┌──────────────────────┐
                                     │  数据落地层            │
                                     │  ClickHouse           │
                                     │  Kafka引擎→MV→MergeTree│
                                     └──────────────────────┘
```

| 层 | 组件 | 职责 | 关键设计点 |
|----|------|------|------------|
| 数据源层 | MySQL 5.7/8.0 | 产生 ROW 格式 Binlog | `binlog_row_image=FULL`、GTID、最小权限账号 |
| CDC 采集层 | Debezium 2.3.x/2.4.x（Kafka Connect） | 无锁快照 + 增量抓取 + DDL 广播 | `snapshot.locking.mode=none`、`include.schema.changes=true` |
| 消息缓冲层 | Kafka 2.8+/3.x + Confluent Schema Registry 7.x | 削峰填谷、持久化、位点回溯 | 单表单 Topic、Avro 序列化、分区数与并发匹配 |
| 数据落地层 | ClickHouse 22.8 LTS+/24.x | 消费、去重合并、分层落地 | 双层表 + MV + ReplacingMergeTree、软删除、FINAL 查询 |

### 2.2 端到端数据流

```
MySQL 数据变更
  → Binlog 生成（ROW+FULL，含 before/after）
  → Debezium 抓取解析（op/before/after/ts_ms/source）
  → 标准化 Avro CDC 消息（Schema Registry 注册）
  → 写入 Kafka Topic（mysql.<db>.<table>）
  → ClickHouse Kafka 引擎表实时拉取
  → 物化视图解析事件（I/U/D/R）
  → ReplacingMergeTree 按 version 合并去重 + 软删除标记
  → 对外提供 FINAL 查询
```

---

## 3. 部署拓扑设计

### 3.1 生产参考拓扑（中等规模：日增量 < 5000 万行）

| 角色 | 实例数 | 规格（建议起步） | 说明 |
|------|--------|------------------|------|
| MySQL | 1 主 +（≥1 从） | 按现有业务 | 建议在从库开启 `log_slave_updates` 后从从库抓取，降低主库压力 |
| Kafka Broker | 3 | 8C32G / SSD ≥ 1TB | 保证 RF=3，容忍单 Broker 故障 |
| ZooKeeper / KRaft | 3 | 4C8G | Kafka 3.x 推荐 KRaft 模式 |
| Schema Registry | 2 | 4C8G | 双实例高可用，共享 `_schemas` topic |
| Kafka Connect（Debezium） | 2~3 | 8C16G | 分布式模式，`tasks.max` 按表数与吞吐调 |
| ClickHouse | 2 分片 ×2 副本（或按需） | 16C64G / SSD | 副本保证高可用，分片保证扩展 |
| 监控栈 | 1 套 | Prometheus + Grafana + Alertmanager | 见 §9 |

> 小规模可先单 Broker/单 Connect/单 CK 起步验证，但生产务必满足「Kafka RF≥3、Connect 分布式≥2、CK 有副本」三条底线。

### 3.2 网络与端口

| 组件 | 默认端口 | 说明 |
|------|----------|------|
| MySQL | 3306 | Debezium 账号连接 |
| Kafka | 9092 | Broker |
| Schema Registry | 8081 | Avro schema 注册/查询 |
| Kafka Connect REST | 8083 | 连接器管理 API |
| ClickHouse | 9000 / 8123 | Native / HTTP |

---

## 4. 数据源层设计（MySQL）

### 4.1 强制前置配置（my.cnf）

```ini
[mysqld]
log_bin = /var/log/mysql/mysql-bin
binlog_format = ROW            # 必须 ROW，禁止 STATEMENT/MIXED
binlog_row_image = FULL        # 必须 FULL，否则 UPDATE 缺旧值导致数据异常
server-id = 1
gtid_mode = ON
enforce_gtid_consistency = ON
expire_logs_days = 7           # binlog 保留 ≥ 快照+消费追赶所需时间
sync_binlog = 1
log_slave_updates = 1          # 主从架构必须开启
```

### 4.2 同步账号（最小权限）

```sql
CREATE USER 'debezium'@'%' IDENTIFIED BY 'StrongPass@123';
GRANT SELECT, REPLICATION SLAVE, REPLICATION CLIENT ON *.* TO 'debezium'@'%';
FLUSH PRIVILEGES;
```

### 4.3 设计约束与避坑

- **binlog 保留期**：`expire_logs_days` 必须覆盖「全量快照耗时 + 最坏情况下游追赶时长」，否则位点被清理将无法续跑，只能重做全量。
- **大表快照**：`snapshot.locking.mode=none` 无锁快照，不锁业务表；但大表快照期间会有较高读负载，建议从从库抓取或错峰执行。
- **无主键表**：见 §6.5，需在 Debezium/CK 侧显式指定唯一键或复合键策略。

---

## 5. CDC 采集层设计（Debezium）

### 5.1 连接器配置（生产模板）

```json
{
  "name": "mysql-business-connector",
  "config": {
    "connector.class": "io.debezium.connector.mysql.MySqlConnector",
    "tasks.max": "4",
    "database.hostname": "192.168.x.x",
    "database.port": "3306",
    "database.user": "debezium",
    "database.password": "StrongPass@123",
    "database.server.id": "1001",
    "database.server.name": "mysql-business",
    "database.include.list": "db_order,db_user",
    "table.include.list": "db_order.t_order,db_user.t_user",
    "database.exclude.list": "mysql,information_schema,performance_schema",
    "snapshot.mode": "initial",
    "snapshot.locking.mode": "none",
    "binlog.include.unknown.events": "false",
    "event.processing.failures.handling": "skip",
    "key.converter": "io.confluent.connect.avro.AvroConverter",
    "key.converter.schema.registry.url": "http://schema-registry:8081",
    "value.converter": "io.confluent.connect.avro.AvroConverter",
    "value.converter.schema.registry.url": "http://schema-registry:8081",
    "include.schema.changes": "true",
    "database.history.kafka.bootstrap.servers": "kafka:9092",
    "database.history.kafka.topic": "schema-changes.mysql-business"
  }
}
```

### 5.2 关键参数设计说明

| 参数 | 取值 | 设计意图 |
|------|------|----------|
| `snapshot.mode` | `initial` | 首次全量快照，之后仅增量 |
| `snapshot.locking.mode` | `none` | 无锁快照，不影响线上业务 |
| `include.schema.changes` | `true` | 开启 DDL 广播，字段增删改可传导 |
| `tasks.max` | 按表/分区数 | 提升并行吞吐，建议与 Topic 分区数匹配 |
| `event.processing.failures.handling` | `skip` | 单条异常事件不中断整体（配合下游告警） |
| `database.history.kafka.topic` | 专用 topic | 保存 schema 历史，重启后恢复表结构上下文 |

> **设计建议（对原文的增强）**：
> - 建议增加 SMT（Single Message Transform）做 `unwrap`（`io.debezium.transforms.ExtractNewRecordState`），把 before/after 展平为扁平记录并注入 `__op`/`__deleted`/`__source_ts_ms` 元字段，与 CK Kafka 表结构对齐（见 §6.1）。
> - `event.processing.failures.handling=skip` 会跳过脏事件，**必须**配合「跳过计数告警」，否则会静默丢数据。生产更保守可用 `fail` + 死信排查。

### 5.3 命名规范

| 对象 | 规范 | 示例 |
|------|------|------|
| Connector 名 | `mysql-<业务域>-connector` | `mysql-order-connector` |
| Topic 名 | `mysql.<db>.<table>` | `mysql.db_order.t_order` |
| Schema 历史 topic | `schema-changes.<server.name>` | `schema-changes.mysql-business` |
| CK 消费组 | `ck-<表>-consumer-group` | `ck-order-consumer-group` |

---

## 6. 消息缓冲层设计（Kafka + Schema Registry）

### 6.1 Topic 设计

- **单表单 Topic**：`mysql.<db>.<table>`，隔离数据、独立位点、独立扩缩容。
- **分区数**：建议与 Debezium `tasks.max` / CK 消费并发匹配；单表分区数 = ceil(峰值写入 TPS / 单分区消费能力)。分区一旦设定不易缩小，宁可略多。
- **副本因子**：RF=3。
- **保留策略**：按「可重放窗口」设定 `retention.ms`（建议 ≥ 3~7 天），核心表可更长或配合分层存储。
- **顺序性**：同一主键的事件需落到同一分区（Debezium 默认以主键做 key hash，保证单键有序）。

### 6.2 Schema Registry 设计

- 统一 Avro 数据结构，自动适配字段新增/修改。
- **兼容性策略**：建议设为 `BACKWARD`（新增可空字段、删列安全），避免破坏下游消费；重大不兼容变更走人工评审 + CK 表结构调整流程（见 §7）。

---

## 7. 数据落地层设计（ClickHouse）

### 7.1 三对象双层表模型

| 对象 | 引擎 | 职责 |
|------|------|------|
| `<表>_kafka` | Kafka | 消费原始 CDC 事件（含 `__op`/`__deleted`/`__source_ts_ms`） |
| `<表>`（正式表） | ReplacingMergeTree(version) | 最终可查询明细，按 version 去重，软删除标记 |
| `mv_<表>` | Materialized View | 自动从 Kafka 表流式写入正式表，做字段映射 |

### 7.2 建表模板（订单表示例）

**① Kafka 消费表**

```sql
CREATE TABLE ods_order_kafka (
    id UInt64,
    order_no String,
    user_id UInt64,
    amount Decimal(18,2),
    status UInt8,
    create_time DateTime,
    update_time DateTime,
    __op String,
    __deleted Bool,
    __source_ts_ms UInt64
)
ENGINE = Kafka
SETTINGS
    kafka_broker_list = 'kafka:9092',
    kafka_topic_list = 'mysql.db_order.t_order',
    kafka_group_id = 'ck-order-consumer-group',
    kafka_format = 'Avro',
    format_avro_schema_registry_url = 'http://schema-registry:8081',
    kafka_skip_broken_messages = 1,
    kafka_max_block_size = 8192;
```

**② 正式落地表**

```sql
CREATE TABLE ods_order (
    id UInt64 COMMENT '主键',
    order_no String COMMENT '订单号',
    user_id UInt64 COMMENT '用户ID',
    amount Decimal(18,2) COMMENT '订单金额',
    status UInt8 COMMENT '订单状态',
    create_time DateTime COMMENT '创建时间',
    update_time DateTime COMMENT '更新时间',
    version UInt64 COMMENT '数据版本时间戳',
    is_deleted UInt8 DEFAULT 0 COMMENT '是否删除 0否1是'
)
ENGINE = ReplacingMergeTree(version)
PRIMARY KEY id
ORDER BY id
PARTITION BY toYYYYMM(create_time)
COMMENT '订单ODS实时同步表';
```

**③ 物化视图**

```sql
CREATE MATERIALIZED VIEW mv_ods_order TO ods_order
AS SELECT
    id, order_no, user_id, amount, status, create_time, update_time,
    __source_ts_ms AS version,
    if(__deleted, 1, 0) AS is_deleted
FROM ods_order_kafka;
```

### 7.3 增删改（DML）语义设计

| MySQL 操作 | CDC op | CK 侧处理 |
|------------|--------|-----------|
| INSERT | `c`/`r` | 新版本写入 |
| UPDATE | `u` | 高 version 覆盖旧版本（ReplacingMergeTree 合并） |
| DELETE | `d` | `is_deleted=1` 软删除标记 |
| REPLACE INTO | `d`+`c` | 解析为删除+插入，天然兼容 |

- **version 选取**：用 `__source_ts_ms`（源库事务时间）作为版本，保证乱序到达时仍按事件真实时序合并。
- **查询规范**：业务查询统一 `... FINAL WHERE is_deleted = 0`，避免读到未合并的旧版本或删除标记行。为高频查询建议构建下游聚合表/DWD 层，减少 FINAL 开销。

### 7.4 DDL 变更处理设计

| DDL 类型 | 处理方式 |
|----------|----------|
| ADD COLUMN（可空/带默认） | Schema Registry 自适应；CK 侧 `ALTER TABLE ... ADD COLUMN`（Kafka 表、正式表、MV 三处同步调整） |
| DROP COLUMN | 评估下游依赖后手动调整；建议保留列位软删，避免破坏历史 |
| MODIFY / CHANGE COLUMN | 类型兼容则自适应；不兼容需人工评审 + 新版本表迁移 |
| CREATE / DROP / TRUNCATE TABLE | 新增表走接入流程；删除/清空表走下线流程 |

> **DDL 流程原则**：MySQL 端 DDL → Schema Registry 演进 → CK 三对象（kafka/正式/MV）一致性调整。三者不同步会导致 MV 写入失败或字段错位，必须纳入变更流程（见执行方案 §DDL 变更 SOP）。

### 7.5 分库分表合并设计

- 多个物理表（`t_order_0..t_order_n`）→ 多 Topic → 一个 CK 正式表。
- 通过多个 MV 或多 Topic 订阅同一 Kafka 表（`kafka_topic_list` 支持多 topic）汇入同一正式表。
- 正式表 `ORDER BY` 需保证全局唯一键（如 `(id)` 或 `(shard_key, id)`），避免跨分表主键冲突。

### 7.6 无主键表设计

- MySQL 无主键表：以「全字段」或业务唯一组合作为 CK `ORDER BY` 去重键。
- 若确实无法唯一标识，则退化为 append-only 明细（不去重），由下游按时间窗口聚合。
- 建议推动业务侧为核心表补主键（治本）。

---

## 8. 高可用与容错设计

| 故障场景 | 机制 | 结果 |
|----------|------|------|
| MySQL 宕机 | Debezium 记录 binlog/GTID 位点，重启后续跑 | 不丢数据 |
| Kafka 宕机 | 消息持久化 + RF=3，恢复后 CK 消费积压 | 不丢数据、延迟恢复 |
| ClickHouse 宕机 | Kafka 保留消息 + 消费组位点，重启回溯消费 | 不丢数据 |
| Connect 节点宕机 | 分布式模式任务自动 rebalance 到其他节点 | 自愈 |
| 单条脏数据 | `kafka_skip_broken_messages` + Debezium skip | 不中断整体（配套告警） |

**一致性保障**：
- 基于 MySQL 事务位点（GTID），事务级一致；
- ReplacingMergeTree 按 `__source_ts_ms` 版本合并，最终唯一正确；
- 支持手动重置消费组位点 / Debezium offset，实现任意时点重同步。

---

## 9. 监控与可观测性设计

### 9.1 核心指标

| 层 | 指标 | 告警阈值（建议） |
|----|------|------------------|
| Debezium | `MilliSecondsBehindSource`（源库延迟）、快照进度、skip 事件数 | 延迟 > 30s；skip 数 > 0 立即告警 |
| Kafka | Topic 堆积（consumer lag）、生产/消费速率、ISR 数 | lag 持续增长；ISR < RF |
| Schema Registry | schema 变更事件、注册失败 | 注册失败 > 0 |
| ClickHouse | `system.kafka_consumers` 异常、MV 写入 QPS、消费延迟、`system.errors` | 消费异常；MV 写入停滞 |

### 9.2 运维查询

```sql
-- CK 消费状态
SELECT * FROM system.kafka_consumers;

-- 有效业务数据（过滤删除）
SELECT * FROM ods_order FINAL WHERE is_deleted = 0;
```

### 9.3 监控栈

- 采集：Debezium/Connect JMX → JMX Exporter；Kafka → kafka_exporter；CK → clickhouse system 表 / ck-exporter。
- 展示告警：Prometheus + Grafana + Alertmanager。

---

## 10. 安全设计

- MySQL 同步账号最小权限（仅 SELECT + REPLICATION）。
- 密码/密钥统一走 Connect 的 config provider（外置化，禁止明文入库/入仓）。
- 网络隔离：CDC 链路置于内网/专有子网，Schema Registry / Connect REST 限制访问来源。
- 敏感字段脱敏：在 Debezium SMT 或 CK MV 层做字段脱敏/掩码（如手机号、身份证）。

---

## 11. 容量与性能设计

| 维度 | 设计要点 |
|------|----------|
| 吞吐 | Debezium `tasks.max`、Topic 分区数、CK `kafka_max_block_size` 三者匹配放大并发 |
| 延迟 | 减小 CK `kafka_poll_max_batch_size`/flush 间隔以降延迟，权衡写放大 |
| 存储 | Kafka 按重放窗口保留；CK 分区（`toYYYYMM`）+ TTL 管理生命周期 |
| FINAL 开销 | 高频查询下沉到 DWD/DWS 聚合表，减少对 ODS FINAL 直查 |

---

## 12. 与 MaterializedMySQL 选型结论 & 演进

- **选原生 MaterializedMySQL**：简单静态业务、无需自定义结构、无复杂 DDL。
- **选本方案**：生产核心业务、需分层建模、DDL 频繁、无主键表、高可用/可重放要求高。

**后续演进**：
1. ODS → DWD → DWS 分层建模，聚合表下沉查询压力；
2. 引入 Flink 做复杂实时计算/多流 join；
3. 死信队列（DLQ）+ 数据质量校验（行数对账、抽样比对）；
4. 跨机房容灾与多活。

---

## 13. 风险与规避清单

| 风险 | 影响 | 规避 |
|------|------|------|
| binlog 未 FULL | UPDATE 缺旧值，数据异常 | 强制 `binlog_row_image=FULL` |
| binlog 过期被清 | 位点丢失需重做全量 | `expire_logs_days` 覆盖追赶窗口 |
| Topic 分区过少 | 消费瓶颈、堆积 | 分区数与并发匹配，宁多勿少 |
| DDL 三对象不同步 | MV 写入失败/字段错位 | DDL 变更 SOP，三处一致调整 |
| 未用 FINAL 查询 | 读到旧版本/删除行 | 统一 `FINAL WHERE is_deleted=0` 或下游聚合 |
| skip 静默丢数 | 数据缺失无感知 | skip 计数告警 + 定期对账 |
| 无主键表未去重 | 数据重复 | 指定唯一键或推动补主键 |
