# MySQL → Debezium → Kafka → ClickHouse 实时 CDC 同步 · 执行方案

> 版本：v1.0 ｜ 日期：2026-07-15
> 配套文档：《01-设计方案.md》
> 目标：把设计方案落地为可执行、可验证、可回滚的分阶段实施步骤

---

## 0. 执行总览

| 阶段 | 名称 | 目标 | 预计工期 | 出口标准（Exit Criteria） |
|------|------|------|----------|----------------------------|
| P0 | 环境准备与评估 | 版本/网络/资源就绪，选定试点表 | 1~2 天 | 前置检查清单全绿 |
| P1 | 基础组件部署 | Kafka/SR/Connect/CK 集群可用 | 2~3 天 | 各组件健康检查通过 |
| P2 | MySQL 侧配置 | binlog/账号就绪 | 0.5 天 | 位点可读、账号可连 |
| P3 | 单表试点打通 | 1 张表端到端跑通 | 1 天 | 全量+增量+DML 验证通过 |
| P4 | DML/DDL 全场景验证 | 增删改+DDL 正确 | 1 天 | 验证用例全过 |
| P5 | 批量接入与灰度 | 多库多表接入 | 2~5 天 | 对账一致、延迟达标 |
| P6 | 监控告警上线 | 可观测闭环 | 1 天 | 关键告警可触发 |
| P7 | 生产验收与交付 | 转生产运维 | 1 天 | 验收单签署、运维手册交付 |

> 里程碑关口：P3（试点打通）与 P5（对账一致）是两个硬关口，未达标不进入下一阶段。

---

## P0 · 环境准备与评估

### P0.1 前置检查清单

- [ ] MySQL 版本 5.7/8.0，具备开启 ROW binlog 的权限与维护窗口
- [ ] 确认从哪个实例抓取（**推荐从库**，降低主库压力，需 `log_slave_updates=1`）
- [ ] 服务器资源到位（见设计方案 §3.1 拓扑）
- [ ] 网络连通性：Connect→MySQL:3306、Connect→Kafka:9092、CK→Kafka:9092、各组件→SR:8081
- [ ] 选定 1 张**试点表**（数据量适中、有主键、有 DML 活动的业务表）
- [ ] 规划命名：Connector 名 / Topic 名 / 消费组 / CK 库表名

### P0.2 版本基线

| 组件 | 版本 |
|------|------|
| MySQL | 5.7 / 8.0 |
| Kafka + Connect | 2.8+ / 3.x |
| Debezium | 2.3.x / 2.4.x |
| Schema Registry | Confluent 7.x |
| ClickHouse | 22.8 LTS+ / 24.x |

---

## P1 · 基础组件部署

> 建议用 Docker Compose / K8s 编排统一部署；以下为部署顺序与验证点。

### P1.1 部署顺序

1. **ZooKeeper/KRaft** → 2. **Kafka Broker（RF=3）** → 3. **Schema Registry（×2）** → 4. **Kafka Connect（Debezium 插件，分布式模式 ×2）** → 5. **ClickHouse（分片+副本）**

### P1.2 各组件健康检查

```bash
# Kafka：列出 broker / 创建测试 topic
kafka-topics.sh --bootstrap-server kafka:9092 --list

# Schema Registry
curl -s http://schema-registry:8081/subjects

# Kafka Connect REST（确认 Debezium 插件已加载）
curl -s http://connect:8083/connector-plugins | grep -i mysql

# ClickHouse
clickhouse-client --query "SELECT version()"
```

### P1.3 出口标准

- [ ] Kafka 三 Broker ISR 正常，可建 RF=3 Topic
- [ ] SR 返回空 subject 列表（正常启动）
- [ ] Connect 插件列表含 `MySqlConnector`
- [ ] CK 各副本可读写、复制正常

---

## P2 · MySQL 侧配置

### P2.1 修改 my.cnf（需重启生效）

```ini
[mysqld]
log_bin = /var/log/mysql/mysql-bin
binlog_format = ROW
binlog_row_image = FULL
server-id = 1
gtid_mode = ON
enforce_gtid_consistency = ON
expire_logs_days = 7
sync_binlog = 1
log_slave_updates = 1
```

### P2.2 创建同步账号

```sql
CREATE USER 'debezium'@'%' IDENTIFIED BY 'StrongPass@123';
GRANT SELECT, REPLICATION SLAVE, REPLICATION CLIENT ON *.* TO 'debezium'@'%';
FLUSH PRIVILEGES;
```

### P2.3 验证

```sql
SHOW VARIABLES LIKE 'binlog_format';       -- ROW
SHOW VARIABLES LIKE 'binlog_row_image';    -- FULL
SHOW VARIABLES LIKE 'gtid_mode';           -- ON
SHOW MASTER STATUS;                        -- 能看到当前 binlog 位点/GTID
```

- [ ] 三项变量符合预期
- [ ] debezium 账号可远程连接并读位点
- [ ] `expire_logs_days` 覆盖预估「全量快照 + 追赶」时长

---

## P3 · 单表试点打通（硬关口）

### P3.1 CK 侧建三对象（以 ods_order 为例）

按设计方案 §7.2 顺序执行：**先建正式表 → 再建 Kafka 表 → 最后建 MV**（MV 一旦建立即开始消费，故正式表须先就绪）。

```sql
-- ① 正式表
CREATE TABLE ods_order (...) ENGINE = ReplacingMergeTree(version)
PRIMARY KEY id ORDER BY id PARTITION BY toYYYYMM(create_time);

-- ② Kafka 消费表
CREATE TABLE ods_order_kafka (...) ENGINE = Kafka SETTINGS ...;

-- ③ 物化视图
CREATE MATERIALIZED VIEW mv_ods_order TO ods_order AS SELECT ... FROM ods_order_kafka;
```

> 完整 DDL 见设计方案 §7.2。

### P3.2 发布 Debezium 连接器

```bash
curl -X POST http://connect:8083/connectors \
  -H "Content-Type: application/json" \
  -d @mysql-order-connector.json
```

（连接器 JSON 见设计方案 §5.1，试点期 `table.include.list` 仅填试点表）

### P3.3 验证连接器状态

```bash
curl -s http://connect:8083/connectors/mysql-business-connector/status
# 期望：connector.state=RUNNING，tasks[].state=RUNNING
```

### P3.4 端到端验证

```sql
-- MySQL 侧：造增量
INSERT INTO db_order.t_order (...) VALUES (...);
UPDATE db_order.t_order SET amount=99.9 WHERE id=<x>;
DELETE FROM db_order.t_order WHERE id=<y>;
```

```sql
-- CK 侧：核对
SELECT count() FROM ods_order FINAL WHERE is_deleted=0;     -- 与 MySQL 行数一致
SELECT * FROM ods_order FINAL WHERE id=<x>;                  -- amount=99.9
SELECT is_deleted FROM ods_order FINAL WHERE id=<y>;         -- =1
SELECT * FROM system.kafka_consumers;                        -- 无异常
```

### P3.5 出口标准（关口）

- [ ] 全量快照行数与 MySQL 一致
- [ ] INSERT/UPDATE/DELETE 在 CK 正确体现
- [ ] `system.kafka_consumers` 无 last_exception
- [ ] 端到端延迟符合预期（P95 ≤ 5s）

---

## P4 · DML/DDL 全场景验证

### P4.1 DML 验证用例

| 用例 | MySQL 操作 | CK 期望 |
|------|-----------|---------|
| 单条插入 | INSERT | 新行出现 |
| 覆盖更新 | UPDATE | 高 version 覆盖，FINAL 取新值 |
| 软删除 | DELETE | is_deleted=1 |
| REPLACE | REPLACE INTO | 删+插，最终为新值 |
| 批量/事务 | 批量 INSERT + 事务内多操作 | 全部落地，无丢失 |

### P4.2 DDL 变更 SOP（标准流程）

> DDL 必须三对象（Kafka 表 / 正式表 / MV）一致调整，否则 MV 写入失败。

**ADD COLUMN（安全，推荐先加可空列）流程：**
1. MySQL 执行 `ALTER TABLE ... ADD COLUMN col ... NULL`；
2. 确认 Schema Registry 新版本注册成功（`BACKWARD` 兼容）；
3. CK 依次执行：
   ```sql
   ALTER TABLE ods_order      ADD COLUMN col <type> DEFAULT <default>;
   -- Kafka 表与 MV 需重建以纳入新列（Kafka 引擎表结构变更通常 DROP+CREATE）
   DROP TABLE mv_ods_order;
   DROP TABLE ods_order_kafka;
   -- 重新 CREATE ods_order_kafka（含 col）、CREATE mv_ods_order（含 col）
   ```
4. 验证新列数据流入正常。

**DROP / MODIFY COLUMN**：先评估下游依赖，走人工评审；不兼容类型变更采用「新版本表 + 迁移」而非原地改。

### P4.3 出口标准

- [ ] DML 5 类用例全过
- [ ] 至少完成 1 次 ADD COLUMN 全链路演练并通过

---

## P5 · 批量接入与灰度（硬关口）

### P5.1 批量接入步骤

1. 扩充连接器 `table.include.list` / `database.include.list`（或按业务域拆多个连接器）；
2. 为每张表建 CK 三对象（可脚本化模板生成）；
3. 分区数与 `tasks.max` 按吞吐重估并对齐；
4. 分批灰度：**先低价值/低频表 → 再核心表**，逐批观察延迟与堆积。

### P5.2 分库分表合并

- 多物理表 Topic → 同一 CK 正式表（`kafka_topic_list` 多 topic 或多 MV）；
- 校验全局唯一键，避免跨分表主键冲突。

### P5.3 数据对账（关口）

```sql
-- 行数对账（按主键区间/分区）
-- MySQL:  SELECT count(*) FROM db_order.t_order;
-- CK:     SELECT count() FROM ods_order FINAL WHERE is_deleted=0;
```

- [ ] 每张表行数对账一致（允许瞬时延迟内的小差异，稳定后归零）
- [ ] 抽样字段值比对一致
- [ ] 全量表延迟稳定达标、无持续堆积

---

## P6 · 监控告警上线

### P6.1 接入指标（见设计方案 §9）

- Debezium/Connect：JMX Exporter → 源库延迟、skip 数、快照进度
- Kafka：kafka_exporter → consumer lag、ISR
- ClickHouse：system 表 / exporter → 消费异常、MV 写入 QPS

### P6.2 关键告警规则（建议）

| 告警 | 条件 | 级别 |
|------|------|------|
| 同步延迟高 | 源库延迟 > 30s 持续 2min | P1 |
| Kafka 堆积 | consumer lag 持续增长 5min | P1 |
| CK 消费异常 | `system.kafka_consumers` 出现 last_exception | P0 |
| skip 丢数 | Debezium/CK skip 计数 > 0 | P0 |
| 组件宕机 | 实例不可达 | P0 |

### P6.3 出口标准

- [ ] Grafana 看板可见全链路指标
- [ ] 人为制造一次异常（如停 Kafka）能触发告警并恢复自愈

---

## P7 · 生产验收与交付

### P7.1 验收清单

- [ ] 所有接入表对账一致、延迟达标
- [ ] 故障演练通过（MySQL/Kafka/CK/Connect 分别宕机→自愈，见下）
- [ ] 监控告警闭环
- [ ] 运维手册 + 应急预案交付

### P7.2 故障演练脚本

| 演练 | 动作 | 期望结果 |
|------|------|----------|
| MySQL 重启 | 停/起 MySQL | Debezium 位点续跑，不丢数据 |
| Kafka 单 Broker 宕 | 停 1 个 Broker | ISR 收敛，消费不中断 |
| CK 重启 | 停/起 CK | 重启后回溯消费积压，最终一致 |
| Connect 节点宕 | 停 1 个 Connect | 任务 rebalance 到其他节点 |

### P7.3 数据重放演练

```bash
# 重置某消费组位点到指定时间/earliest，回溯修复
kafka-consumer-groups.sh --bootstrap-server kafka:9092 \
  --group ck-order-consumer-group --reset-offsets --to-datetime <ts> \
  --topic mysql.db_order.t_order --execute
```

---

## 附录 A · 运维手册（日常）

### 常用命令

```sql
-- CK 消费状态
SELECT * FROM system.kafka_consumers;
-- 有效数据查询（统一 FINAL + 过滤删除）
SELECT * FROM ods_order FINAL WHERE is_deleted = 0;
```

```bash
# 连接器状态 / 重启
curl -s http://connect:8083/connectors/<name>/status
curl -X POST http://connect:8083/connectors/<name>/restart
# 暂停/恢复
curl -X PUT http://connect:8083/connectors/<name>/pause
curl -X PUT http://connect:8083/connectors/<name>/resume
```

### 新表接入 SOP

1. 建 CK 三对象（正式表→Kafka 表→MV）；
2. 连接器 `table.include.list` 增表并 `PUT` 更新配置；
3. 校验状态 RUNNING、对账一致；
4. 纳入监控看板。

### 常见故障处置

| 现象 | 排查 | 处置 |
|------|------|------|
| CK 无新数据 | 查 `system.kafka_consumers` 异常、Topic 有无新消息、MV 是否存在 | 修复 schema/字段错位，必要时重建 MV |
| 延迟持续升高 | 查 consumer lag、Connect task 状态、CK 写入瓶颈 | 加分区/并发、扩 CK 资源 |
| 位点丢失 | binlog 被清理 | 重做该表全量快照 |
| DDL 后写入失败 | 三对象字段不一致 | 按 P4.2 SOP 补齐 |

---

## 附录 B · 上线避坑清单（强约束）

- [ ] `binlog_row_image=FULL`（否则 UPDATE 缺旧值）
- [ ] 禁用 STATEMENT/MIXED binlog
- [ ] Kafka Topic 分区数与 Debezium 任务数匹配
- [ ] Kafka 落盘持久化，禁止关闭
- [ ] 业务查询统一 `FINAL` + `is_deleted=0`
- [ ] DDL 变更三对象一致调整
- [ ] `event.processing.failures.handling=skip` / `kafka_skip_broken_messages` 必须配 skip 告警
- [ ] `expire_logs_days` 覆盖快照+追赶窗口

---

## 附录 C · 交付物清单

| 交付物 | 说明 |
|--------|------|
| 设计方案文档 | 《01-设计方案.md》 |
| 执行方案文档 | 本文档 |
| 连接器配置 | `mysql-*-connector.json`（各业务域） |
| CK 建表脚本 | 各表三对象 SQL（模板化） |
| 监控看板 | Grafana Dashboard JSON |
| 运维手册 | 附录 A + 应急预案 |
| 对账脚本 | 行数/抽样比对脚本 |
